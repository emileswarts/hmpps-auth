spring:
  flyway:
    locations: classpath:db/auth,db/dev/data/auth

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        jdbc:
          lob:
            non_contextual_creation: true

